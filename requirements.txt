# Core (self-contained training + sampling — no APIs, no pretrained weights)
torch>=2.2
numpy
pyyaml
tqdm
imageio
imageio-ffmpeg        # enables .mp4 output
opencv-python         # only needed for the "folder" dataset (your own videos)
